name: Example
on: push
jobs:
  echo:
    runs-on: ubuntu-latest
    
    steps:
      - uses: actions/checkout@v2

      - id: files
        uses: jitterbit/get-changed-files@v1

      - run: touch text.txt
      
      - run: for changed_file in ${{ steps.files.outputs.all }}; do echo -n -e ${changed_file} >> text.txt; done

      - uses: pCYSl5EDgo/cat@master
        with:
          path: text.txt

      # - run: echo $(ls)
      #   env:
      #     TEXT: ${{ steps.hello.outputs.text }}