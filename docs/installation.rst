Installation and Loading
========================

Requirements
------------

- Node.js (https://nodejs.org/)

Installation Process
--------------------

To install the project, run:

``npm i novelcovid``

Once installed, it is recommended to load the module via ``require``\ :

``const covid == require('novelcovid')``